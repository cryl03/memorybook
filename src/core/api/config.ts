export const API_BASE_URL = 'https://api.memora.sandboxmb.com/api';

export const API_UPLOADS_BASE_URL = 'https://api.memora.sandboxmb.com';

/**
 * Flujo sin sesión (álbum solo local).
 * false = hay que iniciar sesión para usar la app / API.
 * No borrar el código guest; solo este switch.
 */
export const ALLOW_GUEST_FLOW = false;
